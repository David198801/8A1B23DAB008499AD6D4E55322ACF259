使用排序与limit结合更好

```javascript
SELECT NAME FROM USER ORDER BY CHAR_LENGTH(NAME) DESC LIMIT 1;

SELECT NAME
FROM USER
WHERE CHAR_LENGTH(NAME)=(
    SELECT MAX(CHAR_LENGTH(NAME))FROM USER
)
LIMIT 1;
 
 SELECT MAX(NAME) FROM USER;-- 比较的是字节数LENGTH(NAME)而不是字符数
```

